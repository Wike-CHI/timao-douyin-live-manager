# ============================================
# 提猫直播助手 - Web 应用 Nginx 配置
# ============================================
# 用途：配置 /app/ 路径访问前端应用
# 部署目录：/www/wwwroot/timao-app

# 在你的 server {} 块中添加以下配置：

# 前端应用访问（公网）
location /app/ {
    alias /www/wwwroot/timao-app/;
    index index.html;
    try_files $uri $uri/ /app/index.html;
    
    # 添加响应头
    add_header Cache-Control "public, max-age=3600";
    add_header X-Content-Type-Options "nosniff";
    
    # 支持 SPA 路由
    location ~ ^/app/(.*)$ {
        try_files $uri $uri/ /app/index.html;
    }
}

# 静态资源缓存
location ~* ^/app/assets/.*\.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
    alias /www/wwwroot/timao-app/assets/;
    expires 7d;
    add_header Cache-Control "public, immutable";
}

# ============================================
# 完整示例（可选）
# ============================================
# 如果你想创建一个完全独立的配置文件，可以使用以下模板：

# server {
#     listen 80;
#     server_name 129.211.218.135;
#     
#     # 前端应用
#     location /app/ {
#         alias /www/wwwroot/timao-app/;
#         index index.html;
#         try_files $uri $uri/ /app/index.html;
#     }
#     
#     # API 代理（如果需要）
#     location /api/ {
#         proxy_pass http://127.0.0.1:11111;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

