# 依赖安装说明

**审查人**: 叶维哲  
**Python版本**: 3.11.9

---

## 📋 依赖文件说明

| 文件 | 说明 | 适用场景 |
|------|------|---------|
| `requirements.txt` | 原始完整依赖 | 开发环境，功能齐全 |
| `requirements.simple.txt` | CPU版本（推荐）| 云服务器，无GPU |
| `requirements.all.txt` | GPU版本 | 本地开发，有GPU |

---

## 🚀 云服务器安装（推荐）

### 问题：libcudart.so.11.0 依赖错误

**原因**: torch 默认安装GPU版本，但服务器没有CUDA

**解决**: 使用CPU版本的torch

### 自动安装脚本（推荐）

```bash
# 在服务器上运行
./install-cpu-version.sh
```

### 手动安装

```bash
cd /www/wwwroot/wwwroot/timao-douyin-live-manager/server

# 激活虚拟环境
source ../.venv/bin/activate

# 卸载现有的torch
pip uninstall -y torch torchaudio torchvision

# 安装CPU版本
pip install torch==2.5.1+cpu torchaudio==2.5.1+cpu torchvision==0.20.1+cpu \
    --extra-index-url https://download.pytorch.org/whl/cpu

# 安装其他依赖
pip install -r requirements.simple.txt
```

---

## 🖥️ 本地开发安装

### 有GPU（推荐）

```bash
pip install -r server/requirements.all.txt
```

### 无GPU

```bash
pip install -r server/requirements.simple.txt
```

---

## ✅ 验证安装

```bash
# 检查Python版本
python --version

# 检查torch版本和CUDA
python -c "import torch; print(f'PyTorch版本: {torch.__version__}'); print(f'CUDA可用: {torch.cuda.is_available()}')"

# 检查关键依赖
python -c "import fastapi, uvicorn, sqlalchemy, redis; print('✅ 核心依赖正常')"
```

---

## 🐛 常见问题

### 问题1: libcudart.so.11.0 错误

**错误信息**:
```
OSError: libcudart.so.11.0: cannot open shared object file: No such file or directory
```

**解决**:
```bash
./install-cpu-version.sh
```

### 问题2: 依赖冲突

**解决**:
```bash
# 清理虚拟环境重新安装
rm -rf ../.venv
python -m venv ../.venv
source ../.venv/bin/activate
pip install -r requirements.simple.txt
```

### 问题3: torch版本不匹配

**解决**:
```bash
# 强制重新安装CPU版本
pip install --force-reinstall torch==2.5.1+cpu torchaudio==2.5.1+cpu torchvision==0.20.1+cpu \
    --extra-index-url https://download.pytorch.org/whl/cpu
```

---

## 📊 依赖大小参考

| 版本 | 大小 | 安装时间 |
|------|------|---------|
| CPU版本 | ~2GB | 10-15分钟 |
| GPU版本 | ~4GB | 15-25分钟 |

---

## 🔧 环境要求

- **Python**: 3.11.9（推荐）或 3.8-3.12
- **pip**: 最新版本
- **内存**: 至少 4GB
- **磁盘**: 至少 10GB 可用空间

---

**安装完成后，重启后端服务即可。**

