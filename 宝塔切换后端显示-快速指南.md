# 🚀 宝塔切换后端显示 - 快速指南

**目标**: 访问公网IP时只显示后端启动页面，不显示前端构建的网页

---

## ✅ 后端服务状态

```
✓ 端口 11111: 已占用
✓ 健康检查: 通过
✓ 根路径: 可访问
✓ API文档: 可访问
```

**可以安全进行配置切换！**

---

## 📋 宝塔面板操作步骤（3步完成）

### 步骤 1: 登录宝塔

浏览器打开: `http://你的服务器IP:8888`

### 步骤 2: 找到站点配置

```
宝塔面板 → 网站 → 129.211.218.135 → 设置 → 配置文件
```

### 步骤 3: 替换配置

找到 `server { ... }` 块，替换为以下配置：

```nginx
server {
    listen 80;
    server_name 129.211.218.135;
    
    # 日志配置
    access_log /www/wwwlogs/timao-access.log;
    error_log /www/wwwlogs/timao-error.log;
    
    # 客户端最大上传大小
    client_max_body_size 50M;
    
    # 超时设置
    proxy_connect_timeout 600s;
    proxy_send_timeout 600s;
    proxy_read_timeout 600s;
    
    # 根路径：直接代理到后端
    location / {
        proxy_pass http://127.0.0.1:11111;
        proxy_http_version 1.1;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        proxy_buffering off;
        proxy_cache off;
    }
    
    # API 文档
    location /docs {
        proxy_pass http://127.0.0.1:11111/docs;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    
    location /openapi.json {
        proxy_pass http://127.0.0.1:11111/openapi.json;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    # 健康检查
    location /health {
        proxy_pass http://127.0.0.1:11111/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        access_log off;
    }
    
    # 静态资源
    location /static/ {
        proxy_pass http://127.0.0.1:11111/static/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

**保存 → 重载配置**

---

## 🎯 验证效果

访问: `http://129.211.218.135`

应该看到：

```
🐱 提猫直播助手 · TalkingCat

本地语音转写 + 抖音直播互动 · 隐私不出机

🚀 服务状态
API服务: ✅ 运行中
转录服务: 🔄 待启动

📚 API文档 | 💚 转写状态 | 🎯 Web 界面
```

---

## 🛠️ 辅助命令

```bash
# 检查后端服务状态
bash scripts/部署与运维/check-backend-status.sh

# 查看配置文件
cat nginx-config-backend-only.conf
```

---

## 📚 详细文档

- **完整操作指南**: `docs/宝塔面板-切换后端显示.md`
- **配置文件**: `nginx-config-backend-only.conf`
- **故障排查**: `docs/Nginx配置故障排查.md`

---

## ⚡ 常见问题

### Q: 修改后仍显示旧页面？
**A**: 按 `Ctrl + Shift + R` 强制刷新浏览器

### Q: 显示 502 错误？
**A**: 检查后端服务是否运行
```bash
curl http://127.0.0.1:11111/health
```

### Q: 如何恢复前端显示？
**A**: 将 `location /` 改回指向 `timao-app` 目录

---

**配置文件位置**: `/www/wwwroot/wwwroot/timao-douyin-live-manager/nginx-config-backend-only.conf`

**修改时间**: 2025-11-14

