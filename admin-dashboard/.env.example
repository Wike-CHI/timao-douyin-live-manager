## Admin Dashboard 环境变量示例

# 管理后台开发服务器端口（Vite）
VITE_ADMIN_PORT=10065

# 后端 FastAPI 基地址（用于 fetch / dataProvider）
# 建议与后端环境变量 BACKEND_PORT 保持一致，默认 10090
VITE_FASTAPI_URL=http://127.0.0.1:10090

# 如果不设置上述变量：
# - 端口默认使用 10065
# - 后端地址默认使用 http://127.0.0.1:10090