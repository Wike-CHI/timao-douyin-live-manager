/**
 * 演示模式工具函数（已废弃）
 * 演示模式功能已完全移除，所有函数返回禁用状态
 */

/**
 * 检查是否启用演示模式（已废弃，始终返回 false）
 */
export const isDemoMode = async (): Promise<boolean> => {
  // 演示模式已完全移除
  return false;
};

/**
 * 获取演示模式用户信息（已废弃，始终返回 null）
 */
export const getDemoUserInfo = async () => {
  // 演示模式已完全移除
  return null;
};